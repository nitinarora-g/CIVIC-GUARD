rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Keep it extremely permissive so our backend server.ts Express container can perform all operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
